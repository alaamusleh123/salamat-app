import 'package:flutter/material.dart';
class ValidatorApp extends StatelessWidget {
  const ValidatorApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
       Image(image: AssetImage('images/Ellipse 11.png',)),
      ],
    );
  }
}
